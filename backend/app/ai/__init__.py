"""Travel Intelligence — the AI module.

Level 1 today: a conversation with a travel-shaped personality and nothing else. No entity data
leaves this process. The context builder that changes that lives in `context.py` when Level 2
arrives, so the boundary is one file rather than a habit.
"""
